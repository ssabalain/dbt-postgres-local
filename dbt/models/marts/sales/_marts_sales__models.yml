version: 2

models:

  - name: dim_listing_bookings
    columns:
      - name: listing_booking_id
        data_tests:
          - unique

  - name: fct_listing_bookings
    columns:
      - name: listing_booking_id
        data_tests:
          - unique